docker-build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    # - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - docker info
  script:
      - echo "Building the auth microservice"
      - cd auth
      - docker build -t afakharany/weatherapp-auth:$CI_COMMIT_REF_SLUG . --no-cache
# #   rules:
#     - if: $CI_COMMIT_BRANCH
#       exists:
#         - Dockerfile
